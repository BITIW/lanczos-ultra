[package]
name = "lanczos-ultra"
version = "0.1.0"
edition = "2024"

[dependencies]
image = "0.25.9"
clap = { version = "4.5.54", features = ["derive"] }
rayon = "1.11"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
debug = false
panic = "abort"